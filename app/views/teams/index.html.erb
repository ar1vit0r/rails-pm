<div class="flex justify-between items-center mb-6">
  <h1 class="text-2xl font-bold text-gray-100">Teams</h1>
  <%= link_to "New Team", new_team_path, class: "bg-cyan-600 text-white px-4 py-2 rounded-lg hover:bg-cyan-700" %>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <% @teams.each do |team| %>
    <%= link_to team_path(team), class: "bg-gray-800 border border-gray-700 rounded-lg p-6 hover:border-cyan-500 transition" do %>
      <h2 class="text-lg font-semibold text-gray-100"><%= team.name %></h2>
      <p class="text-gray-400 text-sm mt-1"><%= team.description&.truncate(100) %></p>
      <div class="flex gap-4 mt-4 text-sm text-gray-500">
        <span><%= team.projects.count %> projects</span>
        <span><%= team.users.count %> members</span>
      </div>
    <% end %>
  <% end %>
</div>
